export default class TowerData{
    constructor(id, archtype, barrels, kills){
        this.id = id;
        this.archtype = archtype;
        this.barrels = barrels;
        this.kills = kills;
    }
}